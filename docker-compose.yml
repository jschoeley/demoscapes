networks:
  mynetwork:

services:
  database:
    build:
      context: .
      dockerfile: Dockerfile.database
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME:?MONGO_INITDB_ROOT_USERNAME is required}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD:?MONGO_INITDB_ROOT_PASSWORD is required}
    command: ["mongod", "--auth", "--bind_ip_all"]
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mongosh --quiet --eval \"quit(db.hello().ok ? 0 : 1)\""
        ]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - mynetwork

  db-init:
    build:
      context: .
      dockerfile: Dockerfile.api
    command: ["npm", "run", "init:database"]
    environment:
      MONGO_APP_DATABASE: ${MONGO_APP_DATABASE:?MONGO_APP_DATABASE is required}
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME:?MONGO_INITDB_ROOT_USERNAME is required}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD:?MONGO_INITDB_ROOT_PASSWORD is required}
      MONGO_API_USERNAME: ${MONGO_API_USERNAME:?MONGO_API_USERNAME is required}
      MONGO_API_PASSWORD: ${MONGO_API_PASSWORD:?MONGO_API_PASSWORD is required}
    depends_on:
      database:
        condition: service_healthy
    networks:
      - mynetwork

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - '${API_PORT:?API_PORT is required}:${API_PORT:?API_PORT is required}'
    depends_on:
      database:
        condition: service_healthy
      db-init:
        condition: service_completed_successfully
    environment:
      MONGO_APP_DATABASE: ${MONGO_APP_DATABASE:?MONGO_APP_DATABASE is required}
      MONGO_API_USERNAME: ${MONGO_API_USERNAME:?MONGO_API_USERNAME is required}
      MONGO_API_PASSWORD: ${MONGO_API_PASSWORD:?MONGO_API_PASSWORD is required}
      PORT: ${API_PORT:?API_PORT is required}
    networks:
      - mynetwork

  website:
    build:
      context: .
      dockerfile: Dockerfile.website
    ports:
      - '80:80'
    depends_on:
      - api
    networks:
      - mynetwork
